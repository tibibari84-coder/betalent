# Copy to .env.neon (gitignored) and paste your Neon connection string from the Neon dashboard
# or Vercel → Project → Settings → Environment Variables → DATABASE_URL (production).
#
# Example shape (use your real URL):
# DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require"

DATABASE_URL=""
