/**
 * POST /api/upload/complete
 *
 * Alias for the direct-upload finalize step.
 * Keeps mobile contract path while reusing the main completion logic.
 */
export { POST } from '@/app/api/videos/upload/complete/route';
